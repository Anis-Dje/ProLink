name: prolink_server
description: Pro-Link REST API backend (Dart shelf + Neon Postgres).
publish_to: none
version: 1.0.0

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shelf_static: ^1.1.2
  shelf_cors_headers: ^0.1.5
  shelf_multipart: ^2.0.0
  postgres: ^3.4.4
  dart_jsonwebtoken: ^2.14.0
  bcrypt: ^1.1.3
  uuid: ^4.4.0
  mime: ^1.0.5
  collection: ^1.18.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
